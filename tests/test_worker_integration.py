"""Worker Integration Tests - core modules replaced (worker_loop, prompt_generator, session), skipped"""
import pytest
pytest.skip("Core modules tested here (worker_loop, prompt_generator, session) no longer exist", allow_module_level=True)
