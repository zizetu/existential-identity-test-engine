{
  "comment": "Global defaults for all workers. Communication channels: Telegram (set TG_BOT_TOKEN + TG_CHAT_ID env vars) and tical-chat (set TICAL_CHAT_URL + TICAL_CHAT_KEY). All credentials are read from environment variables at runtime — never hardcode tokens in config files. See tical_code/core/channel.py for the channel class hierarchy.",
  "verify_level": "strict",
  "log_level": "INFO",
  "strict_host_check": true,
  "auto_verify": true,
  "execution_timeout": 300,
  "max_retries": 3,
  "mcp_client": {
    "enabled": true,
    "servers_path": "config/mcp_servers.json"
  }
}
