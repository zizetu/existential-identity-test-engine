{
  "comment": "Global defaults for all workers. Set TG_BOT_TOKEN=your_bot_token and TG_CHAT_ID=your_chat_id in env to enable Telegram communication. The bot token is read from the TG_BOT_TOKEN environment variable at runtime — never hardcode tokens in config files.",
  "verify_level": "strict",
  "log_level": "INFO",
  "strict_host_check": false,
  "auto_verify": true,
  "execution_timeout": 300,
  "max_retries": 3,
  "mcp_client": {
    "enabled": true,
    "servers_path": "config/mcp_servers.json"
  }
}
